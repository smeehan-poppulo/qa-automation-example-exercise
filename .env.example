# Password used when creating ephemeral test accounts in global-setup.
# In CI, inject this via your secrets manager (GitHub Actions secret, Vault, etc.).
# Locally, copy this file to .env and set a value — or omit it to use the fallback.
TEST_ACCOUNT_PASSWORD=
